# Инструкция по настройке подписки - ЧТО НУЖНО СДЕЛАТЬ

## ✅ ЧТО УЖЕ ГОТОВО (сделано автоматически):

1. ✅ Файл `yookassa-webhook.php` подготовлен с правильными настройками
2. ✅ Все страницы и компоненты созданы
3. ✅ API endpoints готовы

## 📋 ЧТО НУЖНО СДЕЛАТЬ ВРУЧНУЮ (3 простых шага):

### ШАГ 1: Добавить переменную в .env.local (1 минута)

Создайте или откройте файл `.env.local` в корне проекта и добавьте:

```env
WEBHOOK_SECRET=1fddaef9da810cd310872383c3d27a05a2c97618ea94f358f3178e1ad23702c5
```

**Где находится файл:** В корне проекта `purep-web`, рядом с `package.json`

### ШАГ 2: Загрузить PHP файл на Beget (2 минуты)

1. Откройте файл `yookassa-webhook.php` (он уже в проекте, готов к использованию)
2. Войдите в панель Beget → Файловый менеджер
3. Перейдите в `/public_html/`
4. Создайте папку `api` (если её нет)
5. Загрузите файл `yookassa-webhook.php` в папку `api/`
6. Проверьте, что файл доступен по адресу: `https://pure-progression.ru/api/yookassa-webhook.php`

**Важно:** Файл уже содержит правильный `WEBHOOK_SECRET`, ничего менять не нужно!

### ШАГ 3: Настроить webhook в Юкассе (2 минуты)

1. Войдите в личный кабинет Юкассы
2. Перейдите: **Настройки** → **Уведомления**
3. Нажмите **"Добавить URL"**
4. Введите URL:
   ```
   https://pure-progression.ru/api/yookassa-webhook.php
   ```
5. Выберите событие: **`payment.succeeded`**
6. Сохраните

**Важно:** Это отдельный webhook для подписок, он не будет конфликтовать с WooCommerce!

## 🎉 ГОТОВО!

После выполнения этих 3 шагов система подписки будет работать!

## 🧪 Как протестировать:

1. Зайдите на `http://localhost:3000/subscribe` (или ваш домен)
2. Выберите план подписки
3. Оплатите тестовой картой Юкассы: `5555 5555 5555 4444`
4. После оплаты проверьте:
   - Подписка появилась в Firebase (коллекция `users`)
   - Доступ открылся к `/workout-builder` и `/my-workouts`

## 📝 Проверка логов:

Если что-то не работает, проверьте логи на Beget:
- Файл: `/public_html/api/yookassa-webhook.log`
- Или в папке, где разместили `yookassa-webhook.php`

## ❓ Если что-то не работает:

1. Проверьте, что файл доступен по URL (откройте в браузере - должна быть ошибка "Method not allowed", это нормально)
2. Проверьте логи webhook
3. Проверьте, что `WEBHOOK_SECRET` одинаковый в `.env.local` и в PHP файле

