# Чеклист для продакшена - Система подписок

## ✅ Переменные окружения

Убедитесь, что в `.env.local` (или в настройках Vercel/хостинга) установлены:

```env
# ЮKassa - обязательные
NEXT_PUBLIC_YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET_KEY=your_secret_key

# Webhook секрет (для безопасности)
WEBHOOK_SECRET=your_webhook_secret
```

**Проверка:**
- ✅ `NEXT_PUBLIC_YOOKASSA_SHOP_ID` - должен быть установлен
- ✅ `YOOKASSA_SECRET_KEY` - должен быть установлен (НЕ тестовый ключ)
- ✅ `WEBHOOK_SECRET` - должен совпадать с тем, что в PHP файле на WordPress

---

## ✅ WordPress Webhook (pure-progression.ru)

**Файл:** `yookassa-subscription-handler-final.php`

**Что проверить:**
1. ✅ Код вставлен в `functions.php` на WordPress сайте (pure-progression.ru)
2. ✅ `$webhookSecret` в PHP файле совпадает с `WEBHOOK_SECRET` в `.env.local`
3. ✅ `$nextjsApiUrl` указывает на `https://pure-progression.com/api/subscription/update`
4. ✅ Webhook URL в личном кабинете Юкассы настроен на: `https://pure-progression.ru/api/yookassa-webhook.php` (или через WooCommerce)

**Текущие значения в PHP:**
- `$webhookSecret = '1fddaef9da810cd310872383c3d27a05a2c97618ea94f358f3178e1ad23702c5'`
- `$nextjsApiUrl = 'https://pure-progression.com/api/subscription/update'`

---

## ✅ Firebase

**Индексы:**
Создайте составной индекс для `pendingPayments`:
- Collection: `pendingPayments`
- Fields: `processed` (Ascending), `userId` (Ascending), `createdAt` (Descending)

**Ссылка для создания:** (будет в логах при первом запросе)

**Проверка:**
- ✅ Firebase проект настроен
- ✅ Правила безопасности настроены
- ✅ Индексы созданы (если нужно)

---

## ✅ ЮKassa настройки

**В личном кабинете Юкассы:**
1. ✅ Сайт `pure-progression.com` добавлен и одобрен
2. ✅ Webhook URL настроен: `https://pure-progression.ru/api/yookassa-webhook.php`
3. ✅ Используются **продакшен** ключи (НЕ тестовые)
4. ✅ Схема оплаты: одностадийная (`capture: true`)

---

## ✅ Next.js приложение

**Проверка:**
- ✅ Все API routes работают
- ✅ Переменные окружения установлены в Vercel/хостинге
- ✅ `return_url` в создании платежа указывает на правильный домен
- ✅ Тестовые кнопки скрыты в продакшене (проверка `process.env.NODE_ENV`)

---

## ✅ Тестирование

**Перед запуском в продакшен:**
1. ✅ Тестовая оплата прошла успешно
2. ✅ Подписка активировалась автоматически
3. ✅ Webhook получил уведомление от Юкассы
4. ✅ Подписка отображается в профиле пользователя
5. ✅ Премиум страницы доступны только с подпиской

---

## ⚠️ Важные замечания

1. **Цены:** Сейчас установлены тестовые цены (1 рубль). Перед продакшеном нужно установить реальные цены в:
   - `src/app/subscribe/page.js` - `SUBSCRIPTION_PLANS`
   - `src/lib/payments.js` - `createSubscription` function

2. **Безопасность:**
   - ✅ `WEBHOOK_SECRET` должен быть длинным случайным строкой
   - ✅ Никогда не коммитьте `.env.local` в git
   - ✅ Используйте продакшен ключи Юкассы

3. **Мониторинг:**
   - Проверяйте логи в Vercel/хостинге
   - Проверяйте логи WordPress (error_log)
   - Мониторьте Firebase для ошибок

---

## 📝 Следующие шаги

1. Установить реальные цены на подписки
2. Убедиться, что все переменные окружения установлены в продакшене
3. Провести финальное тестирование с реальной оплатой
4. Убрать тестовые кнопки из профиля (если нужно)

---

## 🔧 Текущий статус

- ✅ Система подписок работает
- ✅ Автоматическая активация после оплаты
- ✅ Webhook настроен
- ✅ Fallback механизмы работают
- ⏳ Цены - тестовые (1 рубль)
- ⏳ Финальное тестирование в продакшене

