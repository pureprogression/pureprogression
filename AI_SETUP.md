# Настройка AI Assistant

## 🎯 Рекомендуемый вариант: Hugging Face Inference API (БЕСПЛАТНО!)

**Почему Hugging Face:**
- ✅ **БЕСПЛАТНЫЙ** tier с хорошими лимитами
- ✅ Работает из России без VPN
- ✅ Доступ к множеству моделей
- ✅ Простая настройка

### Шаг 1: Получение API ключа Hugging Face

1. Зайдите на **https://huggingface.co**
2. Зарегистрируйтесь или войдите в аккаунт (бесплатно)
3. Перейдите в **Settings** → **Access Tokens**
4. Нажмите **"New token"**
5. Выберите **"Read"** доступ (достаточно для Inference API)
6. Скопируйте токен (начинается с `hf_...`)

### Шаг 2: Добавление ключа в проект

#### Для локальной разработки:

Создайте файл `.env.local` в корне проекта и добавьте:

```env
HUGGINGFACE_API_KEY=hf_ваш_токен_здесь
```

#### Для Vercel (Production):

1. Зайдите на [vercel.com](https://vercel.com)
2. Выберите ваш проект
3. Перейдите в **Settings** → **Environment Variables**
4. Добавьте переменную:
   - **Name:** `HUGGINGFACE_API_KEY`
   - **Value:** ваш токен (начинается с `hf_`)
   - **Environment:** выберите все (Production, Preview, Development)
5. **Важно:** После добавления сделайте **Redeploy** проекта!

### Шаг 3: Перезапуск сервера

После добавления ключа перезапустите сервер разработки:

```bash
npm run dev
```

---

## Альтернативные варианты

### Вариант 2: Google Gemini API

Если Hugging Face недоступен:

1. Перейдите на **https://aistudio.google.com/app/apikey**
2. Создайте API ключ
3. Добавьте в `.env.local`:
   ```env
   GEMINI_API_KEY=AIzaSy_ваш_ключ_здесь
   ```

**Примечание:** Gemini может не работать из России без VPN.

### Вариант 3: Groq API

Если другие варианты недоступны:

1. Перейдите на **https://console.groq.com** (требуется VPN из России)
2. Создайте API ключ
3. Добавьте в `.env.local`:
   ```env
   GROQ_API_KEY=gsk_ваш_ключ_здесь
   ```

**Примечание:** Groq блокирует запросы из России. Для работы на Vercel нужно настроить регион US/EU.

---

## Приоритет использования

1. **Hugging Face** (если есть `HUGGINGFACE_API_KEY`) - **БЕСПЛАТНО**, рекомендуется
2. **Gemini** (если есть `GEMINI_API_KEY`)
3. **Groq** (если есть `GROQ_API_KEY`)

Система автоматически переключается между сервисами, если один не работает.

---

## Готово!

Теперь AI Assistant должен работать. Попробуйте написать запрос в поле ввода, например:
- "Тренировка на ноги для начинающих"
- "Верх тела, 30 минут"
- "Упражнения на пресс"

## ⚠️ Важно о безопасности

- **НЕ используйте** `NEXT_PUBLIC_` префикс для API ключей - они будут видны в браузере!
- Используйте только серверные переменные (`HUGGINGFACE_API_KEY`, `GEMINI_API_KEY`, `GROQ_API_KEY`)
- Ключи хранятся только на сервере и не передаются клиенту
